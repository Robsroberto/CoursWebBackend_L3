
---

## ⚠️  remarques (facultatives, pas bloquantes)

Ces points ne sont **pas des erreurs**, juste des **améliorations possibles** plus tard :

1. Dans `DashboardController`, la logique par `if` est pédagogique
   → plus tard tu pourras introduire `switch` ou mapping par rôle.

2. Les contrôleurs admin sont volontairement moins détaillés
   → c’est bien, car tu les réserves pour des exercices (cf. partie 3/orientation pédagogique).

👉 **Conclusion** : le contenu est validable **sans modification obligatoire**.

---

# 2️⃣ Ensemble COMPLET des contrôleurs

## (Version PROFESSEUR – référence officielle)

Cette partie est **pour toi uniquement** :

* structure finale
* emplacement exact
* contenu complet et cohérent
* version “corrigé / socle”

---

## 📁 Emplacement global

Tous les contrôleurs sont dans :

```
app/Http/Controllers/
```

Organisation recommandée (lisible et professionnelle) :

```
Controllers/
├── ServiceController.php
├── DashboardController.php
├── ReservationController.php
├── MedecinController.php
└── Admin/
    ├── AdminServiceController.php
    ├── AdminReservationController.php
    └── AdminUserController.php
```

---

## 🔹 ServiceController.php (Public)

```php
<?php

namespace App\Http\Controllers;

use App\Models\Service;

class ServiceController extends Controller
{
    public function index()
    {
        $services = Service::where('statut', 'actif')
            ->with('medecin')
            ->get();

        return view('services.index', compact('services'));
    }

    public function show($id)
    {
        $service = Service::with('medecin')->findOrFail($id);
        return view('services.show', compact('service'));
    }
}
```

---

## 🔹 DashboardController.php

```php
<?php

namespace App\Http\Controllers;

use Illuminate\Support\Facades\Auth;

class DashboardController extends Controller
{
    public function index()
    {
        $user = Auth::user();

        if ($user->role === 'admin') {
            return view('dashboard.admin');
        }

        if ($user->role === 'medecin') {
            return view('dashboard.medecin');
        }

        return view('dashboard.patient');
    }
}
```

---

## 🔹 ReservationController.php (Patient)

```php
<?php

namespace App\Http\Controllers;

use App\Models\Reservation;
use App\Models\Service;
use Illuminate\Http\Request;

class ReservationController extends Controller
{
    public function create($service_id)
    {
        $service = Service::findOrFail($service_id);
        return view('reservations.create', compact('service'));
    }

    public function store(Request $request)
    {
        $validated = $request->validate([
            'service_id' => 'required|exists:services,id',
            'date_reservation' => 'required|date|after_or_equal:today',
            'heure_reservation' => 'required',
            'commentaire' => 'nullable|string',
        ]);

        $validated['user_id'] = auth()->id();
        $validated['statut'] = 'en_attente';

        Reservation::create($validated);

        return redirect('/mes-reservations')
            ->with('success', 'Réservation enregistrée.');
    }

    public function myReservations()
    {
        $reservations = Reservation::with('service')
            ->where('user_id', auth()->id())
            ->get();

        return view('reservations.patient_index', compact('reservations'));
    }

    public function cancel($id)
    {
        $reservation = Reservation::findOrFail($id);

        if ($reservation->user_id !== auth()->id()) {
            abort(403);
        }

        if ($reservation->statut !== 'en_attente') {
            return back()->with('error', 'Impossible d’annuler cette réservation.');
        }

        $reservation->update(['statut' => 'annulee']);

        return back()->with('success', 'Réservation annulée.');
    }
}
```

---

## 🔹 MedecinController.php

```php
<?php

namespace App\Http\Controllers;

use App\Models\Service;
use App\Models\Reservation;
use Illuminate\Http\Request;

class MedecinController extends Controller
{
    public function services()
    {
        $services = Service::where('medecin_id', auth()->id())->get();
        return view('medecin.services', compact('services'));
    }

    public function reservations()
    {
        $reservations = Reservation::with(['service', 'user'])
            ->whereHas('service', function ($query) {
                $query->where('medecin_id', auth()->id());
            })
            ->get();

        return view('medecin.reservations', compact('reservations'));
    }

    public function updateStatus(Request $request, $id)
    {
        $validated = $request->validate([
            'statut' => 'required|in:confirmée,annulée,effectuée',
        ]);

        $reservation = Reservation::findOrFail($id);

        if ($reservation->service->medecin_id !== auth()->id()) {
            abort(403);
        }

        $reservation->update($validated);

        return back()->with('success', 'Statut mis à jour.');
    }
}
```

---

## 🔹 AdminServiceController.php

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Service;

class AdminServiceController extends Controller
{
    public function index()
    {
        $services = Service::with('medecin')->get();
        return view('admin.services.index', compact('services'));
    }
}
```

*(Les deux autres contrôleurs admin suivent exactement la même logique : CRUD + supervision globale)*

---

# 3️⃣ Énoncé d’EXERCICES – Contrôleurs ADMIN (pour étudiants)

👉 **Ce texte est directement partageable aux étudiants.**

---

## EXERCICE — Gestion ADMIN des Services Médicaux

### Contexte

L’administrateur est responsable de la gestion globale des services médicaux proposés sur la plateforme.

---

### Travail demandé

1. Créer un contrôleur :

```bash
php artisan make:controller Admin/AdminServiceController
```

2. Implémenter les méthodes suivantes :

* `index()` : afficher tous les services
* `create()` : afficher le formulaire d’ajout
* `store()` : enregistrer un service
* `edit($id)` : afficher le formulaire de modification
* `update(Request $request, $id)` : modifier un service
* `destroy($id)` : supprimer un service

---

### Contraintes obligatoires

* utiliser le modèle `Service`
* utiliser la validation Laravel
* rediriger avec un message de confirmation
* respecter la structure MVC
* utiliser les vues :

  ```
  admin/services/index.blade.php
  admin/services/create.blade.php
  admin/services/edit.blade.php
  ```

---

### Critères d’évaluation

* respect de l’architecture Laravel
* clarté du contrôleur
* validation correcte
* routes fonctionnelles
* code lisible et structuré

---
Parfait.
Je vais procéder **clairement et dans l’ordre**, comme tu l’as demandé :

1. **Donner les 2 contrôleurs ADMIN restants (version PROFESSEUR – complète)**
2. **Poser les bases du Chapitre 7** : objectifs, logique, prérequis, sans encore entrer dans l’implémentation détaillée (cela viendra ensuite)

Aucun raccourci, tout est cohérent avec ce que tu as déjà validé.

---

# 1️⃣ Contrôleurs ADMIN restants (Version PROFESSEUR – référence)

Ces contrôleurs complètent **logiquement** le Chapitre 6 et servent de **socle officiel** pour toi.

Ils ne sont **pas encore** à donner tels quels aux étudiants (ils serviront de corrigé ou de référence).

---

## 📁 Rappel de l’emplacement

```
app/Http/Controllers/Admin/
├── AdminServiceController.php
├── AdminReservationController.php
└── AdminUserController.php
```

Nous donnons maintenant les **2 derniers**.

---

## 🔹 AdminReservationController.php

### (Supervision globale des réservations)

### Rôle pédagogique

* vue globale côté admin
* différence entre *“mes réservations”* et *“toutes les réservations”*
* lecture uniquement (pas de logique métier lourde)

---

### Code complet

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Reservation;

class AdminReservationController extends Controller
{
    /**
     * Afficher toutes les réservations du système
     */
    public function index()
    {
        $reservations = Reservation::with([
            'service',
            'user',
            'service.medecin'
        ])->orderBy('date_reservation', 'desc')->get();

        return view('admin.reservations.index', compact('reservations'));
    }

    /**
     * Afficher le détail d’une réservation
     */
    public function show($id)
    {
        $reservation = Reservation::with([
            'service',
            'user',
            'service.medecin'
        ])->findOrFail($id);

        return view('admin.reservations.show', compact('reservation'));
    }
}
```

---

### Vues attendues

```
resources/views/admin/reservations/
├── index.blade.php
└── show.blade.php
```

---

### Points importants à expliquer plus tard aux étudiants

* pourquoi `with()` est indispensable ici
* différence entre admin / patient / médecin
* pourquoi l’admin **ne modifie pas** directement le statut (choix pédagogique)

---

## 🔹 AdminUserController.php

### (Gestion des utilisateurs et des rôles)

### Rôle pédagogique

* gestion des utilisateurs
* introduction **douce** à la notion de rôle
* préparation directe du Chapitre 7

---

### Code complet

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\User;
use Illuminate\Http\Request;

class AdminUserController extends Controller
{
    /**
     * Liste de tous les utilisateurs
     */
    public function index()
    {
        $users = User::orderBy('name')->get();
        return view('admin.users.index', compact('users'));
    }

    /**
     * Formulaire de modification du rôle
     */
    public function edit($id)
    {
        $user = User::findOrFail($id);
        return view('admin.users.edit', compact('user'));
    }

    /**
     * Mise à jour du rôle d’un utilisateur
     */
    public function update(Request $request, $id)
    {
        $validated = $request->validate([
            'role' => 'required|in:admin,medecin,patient',
        ]);

        $user = User::findOrFail($id);
        $user->update($validated);

        return redirect()->route('admin.users.index')
            ->with('success', 'Rôle utilisateur mis à jour.');
    }
}
```

---

### Vues attendues

```
resources/views/admin/users/
├── index.blade.php
└── edit.blade.php
```

---

### Pourquoi ce contrôleur est stratégique

* il **introduit le rôle sans middleware**
* il permet de **voir le besoin réel** de sécurisation
* il rend le Chapitre 7 **naturel et logique**

---

# 2️⃣ Préparation du CHAPITRE 7

## Middlewares, Rôles et Sécurisation de l’Application

On ne code pas encore ici.
On **prépare le terrain pédagogique**, exactement comme tu l’as fait pour les autres chapitres.

---

## 🎯 Objectifs pédagogiques du Chapitre 7

À la fin du chapitre 7, l’étudiant doit être capable de :

* comprendre ce qu’est un **middleware**
* expliquer pourquoi on ne met pas la sécurité dans les contrôleurs
* protéger des routes selon le rôle
* empêcher l’accès non autorisé
* rediriger correctement après connexion
* sécuriser l’application sans la casser

---

## 🧠 Problème réel à poser aux étudiants (situation déclencheur)

Actuellement :

* tout le monde peut accéder à `/dashboard/admin`
* un patient peut voir les pages admin
* un médecin peut modifier des utilisateurs
* aucune vérification n’existe

Question clé à poser :

> “Comment empêcher un utilisateur non autorisé d’accéder à certaines pages ?”

👉 Réponse : **middleware**

---

## 🧱 Concepts clés à introduire dans le Chapitre 7

Ces notions devront être **expliquées avant le code** :

1. Qu’est-ce qu’un middleware
2. Pourquoi ne pas mettre la sécurité dans les contrôleurs
3. Différence entre :

   * authentification
   * autorisation
4. Cycle d’une requête HTTP dans Laravel
5. Où intervient un middleware
6. Middleware global vs middleware de route
7. Middleware par rôle

---

## 📌 Pré-requis déjà acquis (important)

Grâce aux chapitres précédents, les étudiants savent déjà :

* ce qu’est une route
* ce qu’est un contrôleur
* ce qu’est un utilisateur
* ce qu’est un rôle (conceptuellement)
* comment Laravel retourne une vue
* comment une session fonctionne (sans le savoir encore)

👉 Le Chapitre 7 s’appuie **directement** sur ces acquis.

---

## 🗺️ Découpage logique du Chapitre 7 (proposition)

Quand tu voudras qu’on le rédige complètement :

1. Introduction aux middlewares
2. Le cycle d’une requête Laravel
3. Authentification vs Autorisation
4. Middleware `auth`
5. Création d’un middleware `role`
6. Protection des routes
7. Redirection selon rôle
8. Tests et erreurs courantes
9. Sécurité minimale attendue en production

---

## 🔜 Prochaine étape possible (au choix)

Tu peux maintenant me dire :

1. **“On écrit le Chapitre 7 version ÉTUDIANT”**
2. **“Commence par expliquer les middlewares sans code”**
3. **“On prépare d’abord les routes sécurisées”**
4. **“Je veux l’énoncé d’un exercice sur les middlewares”**

Dis-moi simplement par quoi tu veux commencer, et on enchaîne proprement.

